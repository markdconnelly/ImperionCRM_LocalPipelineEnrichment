# Scheduled-task registry

The pipeline is **many small scheduled tasks** (one per sync cmdlet), not a monolith
(CLAUDE.md §1). Each runs under the dedicated gMSA/service account, "run whether logged on
or not." Register/update them with the **`Register-ImperionTask`** cmdlet (idempotent). Each
task command is `pwsh -Command "Import-Module ImperionPipeline; Initialize-ImperionContext;
<cmdlet>"`.

> **Every collector is a registered cmdlet (epic #286, ADR-0007).** The loose
> `scheduled-tasks/*.task.ps1` recipes were promoted to exported `Invoke-Imperion*Sync`
> cmdlets and the whole catalog is now in `Register-ImperionTask`'s `$tasks` array — so a
> single `Register-ImperionTask` run on a provisioned host registers all of them. Each is
> dormant-safe (fails closed: logs + exits on a missing credential row or unapplied bronze
> migration). The `$tasks` schema is `-Daily` only; rows whose suggested cadence is sub-daily
> (tickets 15–30 min, comms hourly) register daily as bulk catch-up until a repetition field
> is added (#286).

| Task | Cmdlet | Suggested cadence | Notes |
| --- | --- | --- | --- |
| Entra service principals → IT Glue | `Invoke-ImperionServicePrincipalSync` | daily | partner tenant; per-client onboarding-app fan-out optional |
| Azure + Sentinel inventory | `Invoke-ImperionAzureInventorySync` | daily | skips workspaces without Sentinel |
| Azure inventory (per-entity get → post) | `Invoke-ImperionAzureResourceInventorySync` | daily | subscriptions → resource groups → resources via `Set-ImperionAzure*ToBronze`; mgmt-groups stay with the sync cmdlet above |
| **Azure cloud_asset silver merge (CMDB)** | `Invoke-ImperionCloudAssetMerge` | daily (AFTER the `azure/cloud-resources` collector) | folds `cloud_resources` (azure_arm) bronze → provider-agnostic silver `cloud_asset` the CMDB cloud CI arm reads; category normalized via the pinned namespace map; merge-co-locates-with-ingestion (ADR-0026 migration 2), the on-prem twin of the cloud's `mergeCloudAssetSources` (ceded via Pipeline #135). Idempotent `ON CONFLICT (provider, external_id)` upsert; unmapped-tenant rows kept with NULL account_id (CMDB view filters). GATED — logs+exits until front-end migration 0139 applied + collector has run; CMDB cloud empty until `account_tenant` mapped. **Registered by `Register-ImperionTask` as `\Imperion\Imperion-CloudAssetMerge` @ 03:10** — operator: re-run `Register-ImperionTask` once, then `Start-ScheduledTask -TaskName 'Imperion-CloudAssetMerge' -TaskPath '\Imperion\'` to run on demand |
| Sentinel objects (per-entity get → post) | `Invoke-ImperionSentinelSync` | daily | `Get-ImperionSentinelObject` → `Set-ImperionSentinelToBronze` (multi-table router, sentinel_* set); existing Reader grant only (#97) |
| Secure Score | `Invoke-ImperionSecureScoreSync` | daily | overall + control profiles |
| Security-posture policies + drift | `Invoke-ImperionPolicySync` | daily | CA/Intune/device-config/Autopilot/Defender; drift vs golden |
| **Posture silver merge (all tenants)** | `Invoke-ImperionPostureMerge` | daily 03:20 (after SecureScore + PolicySync) | classifies posture_policy + rolls up tenant_posture, unmapped tenants included (ADR-0010); **operator: re-run `Register-ImperionTask` once to register** |
| **Posture snapshots (Imperion Secure Score)** | `Invoke-ImperionPostureSnapshot` | daily 03:40 (after PostureMerge) — self-gates to calendar quarters | immutable posture_snapshot(+pillar) per mapped account, Score Model v1 parity-pinned to frontend imperion-score.ts (ADR-0011); on-demand/QBR triggers bypass the gate; registered by the same `Register-ImperionTask` run |
| Autotask contracts | `Invoke-ImperionAutotaskContractSync` | daily | incremental on `lastModifiedDateTime` (`IMPERION_AUTOTASK_CONTRACT_SINCE_DAYS`, default 7; `-SinceDays 0` = full backfill). **Registered as `\Imperion\Imperion-AutotaskContracts` @ 01:15** (#287, ADR-0007 promotion) |
| Autotask tickets | `Invoke-ImperionAutotaskTicketSync` | daily bulk catch-up (doc target 15–30 min) | incremental on `lastActivityDate` (`IMPERION_AUTOTASK_TICKET_SINCE_DAYS`, default 1). **Registered as `\Imperion\Imperion-AutotaskTickets` @ 01:30** (#287). Real-time tickets arrive via cloud-Pipeline webhooks; this is the bulk reconcile. Sub-daily trigger cadence deferred (epic #286 — `$tasks` schema needs a repetition field) |
| M365 users | `Invoke-ImperionM365UserSync` | daily | → `m365_contacts` (ADR-0039 shape); per-tenant onboarding-app fan-out via `IMPERION_M365_TENANT_IDS` |
| M365 devices | `Invoke-ImperionM365DeviceSync` | daily | → `m365_devices` (ADR-0039 shape); per-tenant onboarding-app fan-out via `IMPERION_M365_TENANT_IDS` |
| **M365 directory silver merge (group membership → enrichment)** | `Invoke-ImperionM365DirectoryMerge` | daily (AFTER the `m365/users` + `m365/entra-groups` + `m365/entra-group-members` collectors) | folds Entra group membership into silver `contact_enrichment` (`directory_groups` fact, source `m365_directory`, lawful_basis `legitimate_interest`); merge-co-locates-with-ingestion (ADR-0026), the on-prem twin of the cloud's `mergeDirectoryGroups` (ceded via Pipeline #134). Idempotent full-replace; GATED — logs+exits until front-end migration 0079 (`m365_groups`/`m365_group_members`) is applied + collectors have run. **Registered by `Register-ImperionTask` as `\Imperion\Imperion-M365DirectoryMerge` @ 03:15** — operator: re-run `Register-ImperionTask` once to register |
| Intune device compliance | `Invoke-ImperionIntuneDeviceSync` | daily | → `intune_managed_devices` (flat compliance columns, frontend ADR-0051 d6, issue #75); GATED — pending front-end bronze migration; single-tenant default, fan-out via `IMPERION_M365_TENANT_IDS` |
| Intune managed apps | `Invoke-ImperionIntuneAppSync` | daily | → `intune_managed_apps` (flat publishing/assignment columns, issue #143 / frontend ImperionCRM #261); GATED — pending DeviceManagementApps.Read.All grant + front-end bronze migration; single-tenant default, fan-out via `IMPERION_M365_TENANT_IDS` |
| Entra domains (tenant hygiene) | `Invoke-ImperionEntraDomainSync` | daily | → `entra_domains` (issue #219/#142 / migration 0136, `Domain.Read.All`); table prod-applied, logs+exits until a tenant is registered/consented; single-tenant default, per-tenant onboarding-app fan-out via `IMPERION_M365_TENANT_IDS` |
| Entra app registrations (tenant hygiene) | `Invoke-ImperionEntraAppRegistrationSync` | daily | → `entra_app_registrations` (issue #219/#142 / migration 0136, `Application.Read.All`); credential count + earliest expiry + has-expired are the hygiene signal; table prod-applied, logs+exits until a tenant is registered/consented; per-tenant onboarding-app fan-out via `IMPERION_M365_TENANT_IDS` |
| Entra role assignments (tenant hygiene) | `Invoke-ImperionEntraRoleAssignmentSync` | daily | → `entra_role_assignments` (issue #219/#142 / migration 0136, `RoleManagement.Read.Directory`, `$expand=roleDefinition,principal`); is_privileged privileged-membership signal; table prod-applied, logs+exits until a tenant is registered/consented; per-tenant onboarding-app fan-out via `IMPERION_M365_TENANT_IDS` |
| Sensitivity labels (info protection) | `Invoke-ImperionSensitivityLabelSync` | daily | → `sensitivity_labels` (issue #141 / front-end #259, `SensitivityLabels.Read.All`); classification taxonomy; benchmark-vs-golden runs in front-end posture merge (#259); GATED — logs+exits until #259 migration lands; per-tenant onboarding-app fan-out via `IMPERION_M365_TENANT_IDS` |
| Custom security attribute definitions (info protection) | `Invoke-ImperionCustomSecurityAttributeSync` | daily | → `custom_security_attribute_definitions` (issue #141 / front-end #259, `CustomSecAttributeDefinition.Read.All`, `$expand=allowedValues`); DEFINITIONS only (assignments deferred, PII); also needs **Attribute Definition Reader** role; GATED — logs+exits until #259 migration lands; per-tenant onboarding-app fan-out via `IMPERION_M365_TENANT_IDS` |
| M365 mail (cross-org) | `Invoke-ImperionM365MailSync` | hourly | → `m365_mail_messages` (migration 0065, source `m365_email`); DOUBLE-GATED — `IMPERION_M365_MAILBOXES`/`IMPERION_M365_CLIENT_DOMAINS` env config + 0065 prod apply (docs/integrations/m365-communications.md) |
| M365 Teams chats (cross-org) | `Invoke-ImperionM365TeamsChatSync` | hourly | → `m365_teams_chats` (migration 0065, source `m365_teams`, collector `user` → `user_upn`); TRIPLE-GATED — env config + 0065 prod apply + Microsoft protected-API approval for chat reads |
| M365 Teams meetings (cross-org) | `Invoke-ImperionM365TeamsMeetingSync` | every 4h | → `m365_teams_meetings` (migration 0065, source `m365_teams`); DOUBLE-GATED — env config + 0065 prod apply (calendar reads are NOT protected-API gated) |
| **Scoped interaction mail (allowlist↔client)** | `Invoke-ImperionScopedInteractionMailSync` | hourly | `Get-ImperionScopedInteractionMail` → `Set-ImperionScopedInteractionMailToBronze` (→ `m365_email`, migration **0120** SHIPPED + prod-applied, source `m365_email`; ADR-0022, issue #199). MESSAGE-grain; distinct from the cross-org `m365_mail_messages` (0065) path. SCOPED AT COLLECTION — only allowlisted-principal (config `%ProgramData%\Imperion\interaction-allowlist.json`, Derek/Mark) ↔ client (silver `contact`/`account`) mail lands; internal-only + non-client dropped (lawful basis, §8). Read-only Graph (cert SP, no new app/secret). **DORMANT** until the allowlist json + Mail.Read consent (Mark); no allowlist → log+exit. Subjects/addresses never logged (counts only). CONFIRM-BEFORE-LIVE on Graph field shapes (docs/integrations/scoped-interaction-capture.md) |
| **Scoped interaction Teams (allowlist↔client)** | `Invoke-ImperionScopedInteractionTeamsSync` | hourly | `Get-ImperionScopedInteractionTeams` → `Set-ImperionScopedInteractionTeamsToBronze` (→ `m365_teams`, migration **0120** SHIPPED + prod-applied, source `m365_teams`; ADR-0022, #199). MESSAGE-grain chat messages from in-scope chats only. TRIPLE-GATED — allowlist json + chat read consent + Microsoft protected-API approval for `/chats`+messages (mail path goes first). Read-only Graph. Same scope rule + count-only logging as the mail task. CONFIRM-BEFORE-LIVE (docs/integrations/scoped-interaction-capture.md) |
| IT Glue organizations | `Invoke-ImperionITGlueOrganizationSync` | daily | → `itglue_companies` (ADR-0039 shape) |
| IT Glue contacts | `Invoke-ImperionITGlueContactSync` | daily | → `itglue_contacts` (ADR-0039 shape) |
| IT Glue configurations | `Invoke-ImperionITGlueConfigurationSync` | daily | → `itglue_devices` (ADR-0039 shape) |
| IT Glue full export → Postgres | `Invoke-ImperionITGlueExport` | daily/12h | per-type + relationships; ad-hoc slices via `Invoke-ImperionITGlueExportToBronze` |
| Telivy assessments | `Invoke-ImperionTelivyReportSync` | daily | → `televy_reports` (ADR-0039 shape) |
| Dark Web ID compromises | `Invoke-ImperionDarkWebIdCompromiseSync` | daily | company credential from Key Vault (`conn-company-darkwebid`) |
| DocuSign envelopes | `Invoke-ImperionDocuSignEnvelopeSync` | daily | → `docusign_contracts` (standard envelope); GATED — logs+exits until `docusign-token`/`docusign-account-id` provisioned (docs/integrations/docusign.md) |
| UniFi devices | `Invoke-ImperionUniFiDeviceSync` | daily | → `unifi_devices`; DOUBLE-GATED — Key Vault `conn-company-unifi` + pending front-end bronze migration (docs/integrations/unifi.md) |
| Plaud recordings | `Invoke-ImperionPlaudRecordingSync` | daily | → `plaud_recordings`; DOUBLE-GATED — `plaud-oauth-token` freshness (fail-loudly re-auth rule) + pending front-end bronze migration (docs/integrations/plaud.md) |
| Kaseya proposals/contracts/tickets | `Invoke-ImperionKaseyaImport` | hourly–daily | bulk upsert, watermarked; Proposals branch delegates to the KQM collector (#98) |
| KQM opportunities | `Invoke-ImperionKqmOpportunitySync` | daily | → `kqm_opportunities` (quote header, migration 0083); GATED — logs+exits until `kqm-api-key`/Key Vault `KQM-API-Key` reachable; URLs are secret-bearing (?apikey=), never logged; shape verified (spike #427). Won-quote detail = #161 |
| QBO purchases (Check/Expense) | `Invoke-ImperionQboPurchaseSync` | daily | → `qbo_purchases` (authoritative payment fact, frontend ADR-0082/ADR-0085; backend #105 reads it to set a timesheet Paid). Simple Start has no Accounts Payable, so `Bill`/`BillPayment` are unavailable — the fact re-targets to the `Purchase` entity (#174). GATED — logs+exits until `qbo-access-token`/`qbo-realm-id` provisioned (QBO app reg, the standing time-tracking blocker); the front-end `qbo_purchases` migration (0092, #526) is SHIPPED. Read-only, amount/payee never logged (docs/integrations/quickbooks-online.md, ADR-0014) |
| **QBO invoices (finance/BI)** | `Invoke-ImperionQboInvoiceSync` | daily | → `qbo_invoices` (revenue billed; A/R when unpaid; ADR-0020, issue #197). Read-only over the shared `Invoke-ImperionQboRequest` (one connection, many readers — no 2nd app reg). DORMANT until `qbo-access-token`/`qbo-realm-id` provisioned (Mark) AND front-end migration **0120** (front-end #688) applied; fails loudly if `qbo_invoices` absent. Invoice amounts/customer names are financial PII — never logged (counts only) (docs/integrations/quickbooks-online.md) |
| **QBO payments (finance/BI)** | `Invoke-ImperionQboPaymentSync` | daily | → `qbo_payments` (CUSTOMER cash IN against invoices; distinct from `qbo/purchases` cash OUT; ADR-0020, #197). Same gate/posture as QBO invoices; migration **0120**. PII never logged |
| **QBO customers (finance/BI)** | `Invoke-ImperionQboCustomerSync` | daily | → `qbo_customers` (finance-side customer master → join to silver account; ADR-0020, #197). Same gate; migration **0120**. Names/emails/phones/balances PII — never logged |
| **QBO estimates (finance/BI)** | `Invoke-ImperionQboEstimateSync` | daily | → `qbo_estimates` (quotes / committed-but-unbilled pipeline; ADR-0020, #197). Same gate; migration **0120**. PII never logged |
| **QBO chart of accounts — FULL (finance/BI)** | `Invoke-ImperionQboAccountSync` | daily (full backfill — COA is small) | → `qbo_accounts` (FULL chart of accounts, no Classification filter; classification + rollup backbone; ADR-0020, #197). Distinct from `qbo/chart-of-accounts` (expense-only, `qbo_expense_account`); whether that becomes a view over `qbo_accounts` is a front-end migration-author call (ADR-0020 open item). Reference data (not PII). Same gate; migration **0120** |
| **QBO bills — A/P (finance/BI)** | `Invoke-ImperionQboBillSync` | daily | → `qbo_bills` (vendor bills / Accounts Payable; ADR-0020, #197). **CONFIRM-BEFORE-LIVE / graceful degrade:** Simple Start has no A/P, so QBO may return "Feature Not Supported" — the collector WARNS + yields no rows (the table stays dormant; A/P signal carried by `qbo_purchases` + `qbo_accounts`), it does NOT hard-fail. Only credential/transport errors crash. Same gate; migration **0120**. PII never logged |
| **QBO Profit & Loss snapshot (finance/BI)** | `Invoke-ImperionQboProfitAndLossSync` | daily/monthly (idempotent on the period) | → `qbo_profit_and_loss` (period P&L snapshot for revenue/margin BI; ADR-0020, #197). REPORT, not entity — pulls the QBO Reports API (`/reports/ProfitAndLoss`), one immutable snapshot row per period (external_id = period; ADR-0011 snapshot idiom). Window defaults to current month, override via `IMPERION_QBO_PNL_START`/`_END`. Same gate; migration **0120**. Totals never logged |
| **Amazon Business orders (logistics/BI)** | `Invoke-ImperionAmazonBusinessOrderSync` | daily | → `amazon_business_orders` (orders + shipment/tracking + spend; procurement / device-supply BI signal; ADR-0021, issue #198). Front-end bronze migration **0120** SHIPPED + prod-applied (#688) — schema gate CLEAR. Read-only — no order is ever placed/modified. GATED on the credential — logs+exits until `amazon-business-token`/Key Vault `AmazonBusiness-Token` reachable (Mark; plan must include API access). Auth = `Authorization: Bearer` header (URLs not secret-bearing). Pure logistics → straight to Postgres, IT Glue skipped (ADR-0006). Order totals/buyer names are procurement detail — never logged (counts only). Cursor (`nextToken`) paging — CONFIRM-BEFORE-LIVE (docs/integrations/logistics-procurement.md) |
| **CDW orders (logistics/BI)** | `Invoke-ImperionCdwOrderSync` | daily | → `cdw_orders` (orders + PO + shipment/tracking + spend; procurement / device-supply BI signal; ADR-0021, #198). Migration **0120** SHIPPED + prod-applied. Read-only — no order ever placed/modified. GATED on the API key — logs+exits until `cdw-api-key`/Key Vault `CDW-API-Key` reachable (Mark; plan must include API access). Auth = `Authorization: Bearer` header. Straight to Postgres, IT Glue skipped. Totals/account refs never logged. `?page=N` paging — CONFIRM-BEFORE-LIVE (docs/integrations/logistics-procurement.md) |
| **Pax8 companies (procurement/billing)** | `Invoke-ImperionPax8CompanySync` | daily 02:06 | → `pax8_companies` (customer companies under the MSP Pax8 account — the join spine; #279/#1042). Front-end bronze migration **0161** authored (front-end #1052) — apply Mark-gated. Read-only. GATED on the credential — logs+exits until `pax8-client-id`+`pax8-client-secret`/Key Vault `Pax8-Client-Id`+`Pax8-Client-Secret` reachable (Mark). Auth = OAuth2 client-credentials → short-lived bearer (connect helper owns the exchange; secret never logged). Straight to Postgres, IT Glue skipped. Spring `page`/`size` paging — CONFIRM-BEFORE-LIVE (docs/integrations/pax8-integration.md) |
| **Pax8 subscriptions (billing spine)** | `Invoke-ImperionPax8SubscriptionSync` | daily 02:07 | → `pax8_subscriptions` (recurring license commitments; the #1041 cost-recon spine). Migration **0161**; same credential + gate as Pax8 companies. Quantities/terms never logged (counts only) |
| **Pax8 licenses (provision link)** | `Invoke-ImperionPax8LicenseSync` | daily 02:08 | → `pax8_licenses` (seat assignments → agreement line / device via merge #280). Migration **0161**; same credential + gate. `-Path /v1/licenses` is the most likely CONFIRM-BEFORE-LIVE correction (Pax8 may expose under usage-summaries) |
| **Pax8 orders (procure→bill)** | `Invoke-ImperionPax8OrderSync` | daily 02:09 | → `pax8_orders` (procurement events). Migration **0161**; same credential + gate. Order totals never logged |
| Datto RMM devices | `Invoke-ImperionDattoRmmDeviceSync` | daily | → `datto_rmm_devices` (patch/AV state, asset/software inventory; ADR-0018, issue #195). Front-end bronze migration 0119 SHIPPED + prod-applied (#674) — schema gate CLEAR. GATED on the API key — logs+exits until `datto-rmm-api-key`/Key Vault `Datto-RMM-API-Key` reachable (Mark). Auth = API-key→short-lived bearer exchange (connect helper owns it; token never logged). Downstream silver `device` merge precedence `website > datto_rmm > m365 > itglue` is front-end/cloud-Pipeline owned (docs/integrations/datto-rmm.md) |
| Datto BCDR backups | `Invoke-ImperionDattoBcdrBackupSync` | daily | → `datto_bcdr_backups` (per-device backup posture, joins on device_uid; ADR-0018, #195). Migration 0119 SHIPPED + prod-applied. GATED on the API key — logs+exits until `datto-bcdr-api-key`/Key Vault `Datto-BCDR-API-Key` reachable (Mark). Auth = `Authorization: Bearer` header (URLs not secret-bearing) (docs/integrations/datto-bcdr.md) |
| myITprocess recommendations | `Invoke-ImperionMyItProcessRecommendationSync` | daily | → `myitprocess_recommendations` (vCIO roadmap/QBR/assessment recommendations → account; ADR-0018, #195). Migration 0119 SHIPPED + prod-applied. Strategic/account data — flattens straight to Postgres, skips IT Glue (ADR-0006 §2). GATED on the API key — logs+exits until `conn-company-myitprocess` (credential registry, JSON blob → `apiKey`, #292/#299) reachable (Mark). Auth = `api_token` header (docs/integrations/myitprocess.md) |
| **Security incidents + alerts + evidence** | `Invoke-ImperionSecurityIncidentSync` | hourly | `Get-ImperionSecurityIncident` → `Set-ImperionSecurityIncidentToBronze` (multi-table router → `m365_incidents` / `m365_alerts` / `m365_evidence`; source `m365`; ADR-0019, issue #196). FE migration 0119 SHIPPED + prod-applied — schema gate CLEAR. Read-only Graph via the per-client onboarding app (SecurityIncident.Read.All + SecurityAlert.Read.All). Carries `m365_incidents.autotask_ticket_ref` RAW (passthrough) — **CONFIRM-BEFORE-LIVE** on the format before the silver MS↔Autotask stitch (ADR-0019 §1 OPEN ITEM). **DORMANT** until onboarding-app consent + creds (#102); fan-out via `IMPERION_M365_TENANT_IDS` (per-tenant isolation). Distinct from the `defender` task (migration 0076) (docs/integrations/security-incidents.md) |
| **Purview compliance posture + drift** | `Invoke-ImperionPurviewComplianceSync` | daily | `Invoke-ImperionPurviewComplianceSync` → `purview_compliance_policies` + drift vs `purview_compliance_golden` (the existing golden-state/drift engine; ADR-0019 §2, issue #196). **Posture only — NO Purview alerts.** Held OUT of `Invoke-ImperionPostureMerge` silver until the FE widens the `posture_policy.policy_family` CHECK (catalog `Silver=$false`). Migration 0119 SHIPPED + prod-applied. Read-only onboarding-app Graph; **CONFIRM-BEFORE-LIVE** on the Graph surface/fields. **DORMANT** until creds (#102) (docs/integrations/purview-compliance.md) |
| **Security 180-day retention sweep** | `Invoke-ImperionSecurityRetentionSweep` (`Invoke-ImperionSecurityRetentionSweep`, passes `-Confirm:$false`) | daily | prunes `m365_incidents` / `m365_alerts` / `m365_evidence` ONLY (leaf-first; NOT interaction bronze, NOT purview, NOT system-wide) older than 180 days by `collected_at` (ADR-0019 §3, follows ADR-0015 idiom). Safe because Autotask is the durable system of record; the bound also shrinks the standing PII surface. Idempotent, `-WhatIf`-aware, COUNT-ONLY logging (no row content). **DORMANT** until creds (#102); like every write path the first live run is gated — run `-WhatIf` first |
| **Receipt-blob 90-day lifecycle** | `Invoke-ImperionReceiptLifecycle` (the scheduled task passes `-Confirm:$false`) | daily 02:00 | deletes a receipt's storage blob + stamps `receipt_attachment.blob_deleted_at` ONLY when `verified_in_autotask = true` and `uploaded_at` older than 90 days; an aged-but-unverified receipt is RETAINED and flagged (count-only `Warn`), never deleted (frontend ADR-0083, ADR-0015). Idempotent (`-WhatIf`-aware); no PII/paths logged. DOUBLE-GATED — the private storage account + lifecycle (frontend #496) AND migrations 0088–0090 applied (frontend #494, schema live 2026-06-14). Safe deploy-ahead: scans 0 rows on an empty table |
| Onboarding-app access health | (build-order task) | hourly | per-tenant consent/credential expiry, fail-closed surfacing |
| **Gold knowledge + vectorization** | `Invoke-ImperionKnowledgeSync -Vectorize` | nightly 04:30 (after ingests) | composes knowledge_object from silver (incl. FB/IG social interactions — `social` entity type, #127), chunks (v1), embeds via Voyage @ 1024; chunk-hash idempotent — no re-bill (ADR-0009) |
| **OKF semantic drift** | `Invoke-ImperionSemanticDriftSync` | weekly (after merges) | detects live-silver-vs-OKF-bundle drift (column NAMES only — no data/PII) and PROPOSES a sync against the front-end bundle (#175, frontend ADR-0086). DRY-RUN by default — set `IMPERION_SEMANTIC_DRIFT_EXECUTE=1` + provision `IMPERION_GH_TOKEN` to auto-open (fail-closed: no token → log+exit). Never forks/edits the bundle; humans approve. No bundle/DB → clean no-op (docs/integrations/okf-semantic-drift.md) |

> **Grant prerequisite:** front-end migration 0044 grants the local-pipeline SP write on the
> 0038/0043 bronze tables only. The migration-0036 tables the m365/itglue per-object tasks
> target (`m365_contacts`, `m365_devices`, `itglue_companies`, `itglue_contacts`,
> `itglue_devices`) need a follow-up grant migration in the front-end repo before live runs.

## Conventions
- Each task invokes **one** module cmdlet after `Initialize-ImperionContext`. No business
  logic in the task definition.
- Tasks are **idempotent**; overlapping runs are prevented (`-MultipleInstances
  IgnoreNew`).
- Every run emits structured JSON logs to `logs/` (run id, source, counts, duration, cost).
- Cadence per source is documented in each `integrations/` doc; tune without code changes.
