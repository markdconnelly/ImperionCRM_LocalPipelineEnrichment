# Agents

_ImperionCRM_LocalPipelineEnrichment — `docs/agents`_

This repo runs no agents. It *feeds* the orchestrator agent in `ImperionCRM_Backend`. See that repo's `docs/agents/`.

> Part of the system-wide `/docs` standard (front-end `CLAUDE.md` §8). See [../../CLAUDE.md](../../CLAUDE.md).

