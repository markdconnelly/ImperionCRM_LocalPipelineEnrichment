# Api

_ImperionCRM_LocalPipelineEnrichment — `docs/api`_

This repo exposes no inbound API. It is an outbound-only producer. See `ImperionCRM_Backend` for served APIs.

> Part of the system-wide `/docs` standard (front-end `CLAUDE.md` §8). See [../../CLAUDE.md](../../CLAUDE.md).

