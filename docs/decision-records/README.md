# Decision Records

_ImperionCRM_LocalPipelineEnrichment — `docs/decision-records`_

ADRs for every significant decision in this repo, written in the **unified Imperion ADR
format**: a metadata table (Repo `local-pipeline` / Status / Date / Cross-references, plus
Supersedes / Superseded-by / Amends / Amended-by rows where they apply) followed by
`Problem → Context → Options considered → Decision → Consequences (Security / Cost /
Operational impact) → Future considerations → Cross-references`. See
[_template.md](_template.md).

**ADR numbers are per-repo, not global** — the frontend and pipeline repos also have an
ADR-0001+, and they are different decisions. Always qualify a cross-repo reference with
the repo name ("frontend ADR-0043", "backend ADR-0035", "pipeline ADR-0011"); same-repo
references stay plain ("ADR-0002"). Never renumber or rename ADR files.

> Part of the system-wide `/docs` standard (front-end `CLAUDE.md` §8). See [../../CLAUDE.md](../../CLAUDE.md).

## Index

| ID | Title | Status | Date | Decision |
| --- | --- | --- | --- | --- |
| [ADR-0001](ADR-0001-local-bulk-plane-cloud-keeps-webhooks.md) | Local PowerShell pipeline as the bulk-compute plane; cloud Pipeline keeps webhooks | Accepted (completed by pipeline ADR-0011) | 2026-06-08 | Two pipeline planes coexist: the local node owns all scheduled/bulk polling, transforms, and vectorization; the cloud Pipeline keeps only inbound webhook receivers and sub-minute event work. |
| [ADR-0002](ADR-0002-certificate-rooted-unattended-execution.md) | Certificate-rooted unattended execution + read-only-by-default grant | Accepted | 2026-06-08 | One non-exportable machine certificate unlocks the SecretStore (CMS) and is the Entra app credential; grants are read-only by default with explicit writes only to Storage, Postgres, and Key Vault. |
| [ADR-0003](ADR-0003-short-lived-entra-token-postgres.md) | Short-lived Entra token for Postgres (no stored DB password) | Accepted | 2026-06-08 | The cert-backed service principal mints a short-lived AAD token for Azure PostgreSQL per run, over TLS, with a table-scoped Entra role — no DB password is ever stored. |
| [ADR-0004](ADR-0004-vectorization-local-orchestration.md) | Vectorization: local orchestration, pinned pluggable embedding provider | Accepted (amended by ADR-0009) | 2026-06-08 | Embedding orchestration (chunking, dedup, batching, cost accounting, pgvector upsert) runs locally with one pinned model + dimension system-wide; provider was pluggable until ADR-0009 retired that framing. |
| [ADR-0005](ADR-0005-source-catalog-and-table-naming.md) | Source bronze catalog + table-naming reconciliation | Proposed (needs front-end migration sign-off) | 2026-06-08 | Adopt the sibling `{source}_{entity}` physical-table convention; new entities need front-end migrations first; loaders fail loudly on missing tables; `website_*` keeps highest merge precedence. |
| [ADR-0006](ADR-0006-itglue-documentation-relationship-hub.md) | IT Glue as a documentation + relationship hub in the ingestion path | Accepted | 2026-06-08 | Flatten each source to one flat PSObject table that is both documented (and related) in IT Glue and imported as-is into Postgres bronze, with a polymorphic edge table for IT Glue relationships. |
| [ADR-0007](ADR-0007-installed-powershell-module.md) | Package as an installed PowerShell module (not a folder of scripts) | Accepted | 2026-06-08 | Ship the versioned `ImperionPipeline` module with exported cmdlets and `Initialize-ImperionContext`; machine config lives in `%ProgramData%\Imperion\`, outside the module. |
| [ADR-0008](ADR-0008-golden-states-and-drift.md) | Golden states + drift detection for security-posture policies | Accepted | 2026-06-08 | Each security-posture policy type keeps an observed table plus a human-promoted golden baseline; `Get-ImperionPolicyDrift` classifies compliant / drift / ungoverned / missing by hash comparison. |
| [ADR-0009](ADR-0009-settled-embedding-stack-voyage-direct.md) | **The embedding stack is settled: Voyage direct, pinned, built** | Accepted | 2026-06-09 | Call Voyage `voyage-3-large` @ 1024 directly — no provider router — with every constant pinned in `Get-ImperionVectorContract`; chunk-hash idempotency means unchanged content is never re-billed. |
| [ADR-0010](ADR-0010-posture-silver-bulk-merge.md) | Posture silver bulk merge — the scheduled twin of the cloud's on-demand refresh | Accepted | 2026-06-11 | `Invoke-ImperionPostureMerge` (daily 03:20) classifies every tenant's policies into posture_policy and rolls up tenant_posture, one transaction per tenant, with the classification CASE parity-pinned across three implementations. |
| [ADR-0011](ADR-0011-quarterly-posture-snapshots.md) | Quarterly Imperion Secure Score snapshots — daily-gated calendar quarters, parity-pinned score math | Accepted | 2026-06-11 | `Invoke-ImperionPostureSnapshot` (daily 03:40, self-gated to one scheduled snapshot per account per calendar quarter) INSERTs immutable snapshot + pillar rows using `Get-ImperionSecureScore`, the PowerShell twin of the frontend's `imperion-score.ts`. |
| [ADR-0012](ADR-0012-local-service-account-identity.md) | Local service account `.\svc-imperion` as the unattended run-as identity (workgroup host — no gMSA) | Accepted | 2026-06-11 | gMSA impossible without AD, so the nine tasks run as a dedicated local account created by `New-ImperionServiceAccount.ps1` and registered with stored credentials via `Register-ImperionTask -TaskCredential`. |
| [ADR-0013](ADR-0013-meta-business-manager-ingestion.md) | Meta Business Manager ingestion | Accepted | 2026-06-12 | Scheduled on-prem pull of Facebook/Instagram posts, conversations, comments, insights, and lead forms into bronze, flattened straight to Postgres. |
| [ADR-0014](ADR-0014-quickbooks-online-payment-fact.md) | QuickBooks Online purchases as the authoritative payment fact | Proposed | 2026-06-13 | A scheduled on-prem collector bulk-pulls QBO `Purchase` rows (Check/Expense — Simple Start has no AP, #174) into bronze `qbo_purchases` (read-only, authoritative for the payment fact alone); deploy-ahead/gated on the QBO app registration (front-end migration 0092 shipped). |
| [ADR-0015](ADR-0015-receipt-blob-90-day-lifecycle.md) | Receipt-blob 90-day lifecycle, guarded by verified-in-Autotask | Proposed | 2026-06-14 | `Invoke-ImperionReceiptLifecycle` deletes a receipt's storage blob + stamps `blob_deleted_at` only when `verified_in_autotask = true` and `uploaded_at` is older than 90 days; an aged-but-unverified receipt is retained and flagged, never deleted. Idempotent, `-WhatIf`-aware, count-only logging. |
| [ADR-0017](ADR-0017-mileiq-drive-pull.md) | Scheduled per-employee MileIQ drive pull → `mileiq_drive` bronze | Proposed | 2026-06-14 | A scheduled on-prem collector pulls business-classified MileIQ drives per connected employee (from `employee_profile.mileiq_user_id`) into typed bronze `mileiq_drive`; per-employee OAuth (backend custodies the token, this repo reads it), fail-closed/dormant-per-employee, personal drives never enter, no comp data; deploy-ahead/gated on MileIQ creds + backend custody + migrations 0088–0090. |
| ADR-0018 (placeholder, #195) | RMM / managed-estate sources — Datto RMM, Datto BCDR, myITprocess (+ device-precedence revisit) | Proposed | 2026-06-15 | Adopt three managed-estate sources into bronze (`datto_rmm_devices`, `datto_bcdr_backups`, `myitprocess_recommendations`, front-end migration 0119); Datto RMM becomes a strong device authority **above m365/itglue but below `website`** (new order `website > datto_rmm > m365 > itglue`); SecretStore API keys, IT Glue path for the operational sources, fail-loud-on-missing-table. **ADR phase only** — collectors follow once frontend 0119 merges. |
| ADR-0019 (placeholder, #196) | Security incidents (MS↔Autotask correlation) + Purview posture + 180-day security retention | Proposed | 2026-06-15 | Ingest curated Microsoft incident/alert/evidence (`m365_incidents`/`m365_alerts`/`m365_evidence`, FE migration 0119, already prod-applied) read-only; **Autotask = system of record**, correlated via `m365_incidents.autotask_ticket_ref` (format an OPEN ITEM, confirm live); Purview enters **posture only** into `purview_compliance_policies`/`_golden` via the `*_golden`+drift pattern (**alerts NOT ingested**); raw security logs **excluded**; a gated `Invoke-ImperionSecurityRetentionSweep` caps security rows at **180 days only**. **ADR phase only** — collectors + sweep at W30; #196 stays open. |
| [ADR-0022](ADR-0022-scoped-interaction-collector.md) (#199) | Scoped interaction capture — allowlisted principal ↔ client only, config-driven | Accepted | 2026-06-16 | Add a scoped mail + Teams interaction-capture collector pair that filters **at collection** to keep ONLY message-grain communications where a **config-driven allowlisted principal** (Derek/Mark, read from `%ProgramData%\Imperion\interaction-allowlist.json`, never hardcoded) AND a **client counterpart** (resolved against silver `contact`/`account`) are both participants — internal-only + non-client traffic never lands (lawful basis, §8). Lands in bronze `m365_email` / `m365_teams` (front-end migration 0120 / #688, already prod-applied — so **ADR + collectors ship together**). Reuses the cert-SP read-only Graph app (**no new app reg / no new secret**); pure communication → straight to Postgres (IT Glue skipped, ADR-0006); idempotent upsert + content-hash skip; **no write authority ever**. **DORMANT** until the allowlist json + Graph consent (+ Teams protected-API approval) (Mark). No silver/OKF change (bronze landing only). |
| [ADR-0021](ADR-0021-logistics-procurement-amazon-cdw.md) (#198) | Logistics / procurement — Amazon Business + CDW read-only orders pull into bronze | Accepted | 2026-06-15 | Adopt Amazon Business + CDW as read-only logistics/procurement sources: bulk-pull orders + shipment/tracking + spend into bronze `amazon_business_orders` / `cdw_orders` (front-end migration 0120 / #688, already prod-applied — so **ADR + collectors ship together**). Two new read-only connections + COMPANY-credential resolvers (SecretStore-first / Key-Vault-fallback, bearer header — `amazon-business-token` / `cdw-api-key`); pure logistics → straight to Postgres (IT Glue skipped, ADR-0006); idempotent upsert + content-hash skip; **no write authority ever**. Procurement spend becomes a first-class BI signal. **DORMANT** until the new SecretStore credentials are provisioned (Mark). No silver/OKF change (bronze landing only). |
| ADR-0020 (#197) | Finance — QuickBooks Online read-only full-data pull into the intelligence / BI engine | Proposed | 2026-06-15 | Widen QBO from the two payroll/expense slices to a **read-only full finance source for BI**: bulk-pull `Invoice`/`Payment`/`Customer`/`Estimate`/`Bill`/`Account`(full COA)/P&L into bronze `qbo_invoices`/`qbo_payments`/`qbo_customers`/`qbo_estimates`/`qbo_bills`/`qbo_accounts`/`qbo_profit_and_loss` (front-end migration 0120 / #688); **reuse the existing `conn-company-qbo` connection (`Invoke-ImperionQboRequest`) read-only — add read scopes only, no second app reg, no write authority ever**; `qbo_purchases` (0092) reused not duplicated; pure finance → straight to Postgres (IT Glue skipped); Simple Start `Bill`/A/P an OPEN ITEM (confirm live). Finance becomes a first-class account-health / BI signal. **ADR phase only** — collectors follow at W32 (gated on FE 0120 + QBO app-reg read scopes); #197 stays open. |
| [ADR-0026](ADR-0026-merge-co-locates-with-ingestion.md) (placeholder, #239) | Bronze→silver merge co-locates with ingestion (LP owns the merge for LP-ingested sources) | Proposed | 2026-06-18 | Generalize ADR-0010: whichever plane ingests a source's bronze owns its bronze→silver merge — LP merges bulk/scheduled sources here (set-based `Invoke-Imperion*Merge` after the collectors), the cloud Pipeline keeps only live/webhook-driven merges. Migration 1: M365 directory groups → `contact_enrichment` (`Invoke-ImperionM365DirectoryMerge`, #239, cedes the cloud `mergeDirectoryGroups`, Pipeline #134). Migration 2: Azure `cloud_resources` → `cloud_asset` (`Invoke-ImperionCloudAssetMerge`, #241, cedes the cloud `mergeCloudAssetSources`, Pipeline #135). Both copies idempotent so cutover (LP first, cloud removal second) has no gap. |
| ADR-NNNN ([file](ADR-NNNN-module-surface-shrink-private-internals.md), placeholder, #226) | ImperionPipeline module surface-shrink — Private internals, ~30 public entrypoints | Proposed | 2026-06-19 | Lever A of epic #225: reclassify internal building blocks (`Get-*` composers, `Set-*ToBronze` writers, request clients, chunker, embedding client) as **Private** and keep only the ~30 genuine entrypoints in `FunctionsToExport` — visibility change only, internals stay callable in-module and are tested via `InModuleScope`. Lands one source family per micro-PR; first family is `knowledge` (14 internals privatized, the two `Invoke-*` orchestrators stay public). |
| [ADR-0024](ADR-0024-predictive-lead-score.md) (#220) | Predictive (ML) lead score — an LP scheduled pass writing `lead_score (kind='predicted')` | Accepted | 2026-06-17 | Back the `predicted` half of the `lead_score` contract (frontend ADR-0073 d5 / migration 0116) with a **local-pipeline scheduled pass** that reads silver engagement/interaction history, runs an explainable ML model, and idempotently UPSERTs `lead_score (kind='predicted')` (0..100 + per-feature `breakdown` jsonb) — **coexisting with, never replacing**, the rule score. ML/bulk lives in LP by doctrine (system §1, frontend ADR-0042); FE only reads. Degrades to rule-only (predicted row simply absent) until the model clears a quality gate — enough labelled engagement history + a passing offline eval — the gating precondition. No new PII surface (reads existing silver); reuses the scheduled-pass machinery (ADR-0010/0011) + settled Voyage @ 1024 stack (ADR-0009). **ADR phase only** — decides model architecture, not internals; collector/model follow once the quality gate clears. |
