# Runbooks

_ImperionCRM_LocalPipelineEnrichment — `docs/runbooks`_

Operational runbooks (see also `operations/`).

- [`data-in-light-up.md`](data-in-light-up.md) — take a client (or Imperion, client-zero) from
  "integrated on paper" to real prod rows: populate the credential registry → enable collectors
  → run merges → gold re-sync + vectorization (#278).

> Part of the system-wide `/docs` standard (front-end `CLAUDE.md` §8). See [../../CLAUDE.md](../../CLAUDE.md).

